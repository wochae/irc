# IRC 서버 포트폴리오 (ADC L4–L7 정렬)

## 개요
- RFC 기반 IRC 프로토콜을 처리하는 논블로킹 TCP 서버를 설계·구현.
- 이벤트 루프(kqueue)·세션/채널 상태 관리·메시지 파싱·브로드캐스트 최적화에 집중.
- ADC(L4–L7 스위치) 애플리케이션 개발 직무에 맞춘 학습/확장 로드맵 포함.

## 목표
- 고성능·안정성 있는 L7 애플리케이션(IRC) 서버 구현.
- L4 관점(멀티 워커, 스티키 세션, 헬스체크) 디자인을 접목해 서비스 품질·운영성 향상.
- 개선–측정–문서화 사이클을 정착해 제품 완성도 제고.

## 학습/연구 방식
- 표준/원문 중심: RFC·매뉴얼·man 페이지를 기반으로 최소 구현 → 부하 테스트 → 패킷 캡처 → 지표화 → 리팩터 반복.
- 운영 디테일 체득: 논블로킹 I/O, 에지/레벨 트리거, EOF/ERR 처리, 백프레셔/에러 재시도 정책.
- 관측 병행: `tcpdump/wireshark`와 간단 테스트로 코드 변경의 지표 영향 확인.

## 기술 스택
- 언어: C++(C 스타일 소켓 API 중심), 향후 Python(테스트/도구) 보완 계획
- OS/네트워킹: BSD 계열(kqueue), Linux 포팅(epoll) 예정
- 빌드/테스트: `make`, 단위 테스트(`test/`)
- 분석/도구: `tcpdump`, `wireshark`, 간단 벤치/로그
- 형상관리: GitHub 리포지토리 운영(이슈/PR/README/문서)

## 아키텍처
- 패턴: Reactor 패턴 기반 논블로킹 이벤트 루프
- 주요 구성요소(파일)
  - 리액터: `srcs/reactor/SocketReactor.hpp`(이벤트 등록·디스패치)
  - 서버: `srcs/server/IRCServer.{hpp,cpp}`(소켓 수명주기/accept/read/write/에러 처리)
  - 세션: `srcs/session/*`(세션 식별·로그인 상태·연결 관리)
  - 클라이언트: `srcs/client/*`(닉네임/속성·활성 상태)
  - 채널: `srcs/channel/*`(채널 모드·권한·브로드캐스트 경로)
  - 메시지/패킷: `srcs/message/*`, `srcs/packet/*`(파싱·응답 생성)
- 데이터 흐름
  - Accept → 세션 생성 → Read(버퍼) → Parse(IRCMessage) → Route(PacketManager) → State 변경(Session/Client/Channel) → Write(응답)
  - 이벤트: `READ` 데이터, `EOF`/`ERR`에서 자원 정리 및 세션 종료

## 핵심 구현 포인트
- 논블로킹 루프: kqueue 기반 이벤트 루프, `EVFILT_WRITE` 불필요 등록 제거, `EV_EOF` 수신 시 정리 핸들러 호출.
- 입력 처리: `EAGAIN`/부분 읽기 대응, 메시지 경계 관리(IRC 라인 기반).
- 상태 관리: 닉네임·세션·채널 맵 일관 유지, 권한/모드에 따른 라우팅 분기.
- L7 처리: RFC 기반 `NICK`, `USER`, `JOIN`, `PRIVMSG`, `PART`, `QUIT` 등 파싱/응답.
- 브로드캐스트 최적화: 채널 참여자 집합 기반 전송·자원 사용 최적화 고려.

## 안정화 패치 요약
- 이벤트 등록 버그 수정: 불필요한 `EVFILT_WRITE` 등록 제거, `EV_EOF`에서 안전 정리.
- 널 포인터 접근 방지: 메시지 처리 경로에서 초기화/널 체크 강화.
- 세션 수명·전송 안전성: 종료 경로 일원화, 이중 해제/중복 전송 예방.
- 과다 로그 완화: 논블로킹 `accept()`의 `EAGAIN` 상황에서 로그 빈도 제한.
- 객체 초기화 일관화: `IRCMessage` 초기화/clear 보완.

## 테스트·검증
- 단위 테스트: `test/` 폴더의 파서·핵심 로직 테스트 실행(분리 빌드).
- 수동 시나리오
  - 빌드: `make`
  - 실행: 바이너리 인자·포트는 `srcs/main.cpp`를 참고하여 설정
  - 접속: 예) `nc localhost <PORT>` 후 IRC 명령 전송 (`NICK`, `USER`, `JOIN`, `PRIVMSG` 등)
- 패킷 분석: `tcpdump`로 SYN/ACK/데이터/종료 경로 확인, 재전송/윈도우 관찰.
- 관측 로그: 접속 수, 메시지 수, 에러 수, 채널 브로드캐스트 비용 등 카운터화.

## 성능·관측(지표 제안)
- 처리량(QPS/PPS), 평균/99% 지연, 연결 성공률/실패율, CPU/메모리/FD 사용량.
- 브로드캐스트 비용(수신자 수 대비 전송량), 비정상 입력에 대한 방어 성공률.
- 지표/대시보드로 변경의 효과를 커밋과 연결하여 기록.

## ADC(L4–L7) 정렬·로드맵
- 달성 항목
  - L7 프로토콜 처리(IRC), 상태 관리, 논블로킹 서버 설계·디버깅 역량 확보
  - 네트워크 데몬 운영 관점(에러 경로/EOF/버퍼/로그/정리) 체득
- 2–4주 로드맵(수행 순서·수용 기준)
  1) Linux `epoll` 포팅
     - 기준: 단일 워커에서 기능 동일성, `EPOLLIN/ERR/RDHUP` 경로 정확
  2) 멀티 워커(`SO_REUSEPORT`)
     - 기준: 워커별 연결 분배·자원 격리, 과도한 lock 없이 안정 동작
  3) 스티키 세션(5-튜플 해시)
     - 기준: 동일 클라이언트 연결이 동일 워커로 일관 라우팅
  4) 헬스체크(간단 TCP)
     - 기준: 실패 시 제외·연속 성공 시 복구, 로그/지표로 확인
  5) L7 확장(HTTP/1.1·DNS 미니 모듈)
     - 기준: 요청/응답 최소 구현·RFC 준수·에러 방어
  6) 컨테이너화(Dockerfile/Compose)
     - 기준: 재현 가능한 빌드/실행·포트/환경 변수 구성·헬스체크 통합
- 참고 구현 포인트
  - 워커 설계: 프로세스/스레드 선택, 세션/채널 맵의 워커-로컬화, 브로드캐스트 경로 설계
  - 라우팅: 5-튜플을 해시해 워커 결정, 재접속 일관성 테스트

## 데모 방법(예시)
- 빌드: `make`
- 테스트 빌드: `make -C test`
- 실행: 빌드된 바이너리(인자/포트는 `srcs/main.cpp` 확인)
- 클라이언트: `nc` 또는 IRC 클라이언트(irssi/weechat 등)로 접속 후 명령 입력
  - 예: `NICK yournick`, `USER yournick 0 * :Real Name`, `JOIN #room`, `PRIVMSG #room :hello`
- 분석: `tcpdump -i lo0 port <PORT>`로 세션/데이터 흐름 확인

## 성취·배운 점
- 논블로킹 이벤트 루프·상태 관리가 서비스 품질(성능·안정성)에 직결됨을 체감.
- 작은 디테일(EOF 처리·버퍼·로그)이 실제 운영 안정성에 미치는 영향 인지.
- 개선–측정–문서화를 습관화해 변경의 효과를 수치로 증명.

## 링크/참고
- 리포지토리: https://github.com/wochae/irc
- 문서: `README.md`, `goal.md`(학습 목표·체크리스트)
- 테스트: `test/`